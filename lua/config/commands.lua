-- [[ Setting commands ]]
-- See `:help command` for more information on commands.

-- Enable filetype detection, filetype-specific plugins, and indentation.
-- This is a command, not an option, so we use `vim.cmd`.
-- See `:help filetype`
vim.cmd 'filetype plugin indent on'
